#include <stdio.h>
#include <string.h>

int main() {
    FILE *arq;
    char a[77]="aluca e poderosa de mais iauu";
    arq=fopen("arq.txt","r");

    char  lido[100],lido2[20];
    int idade;
    fscanf(arq,"%s %d",lido,&idade);

    printf("%s", lido);
    printf("%s", lido2);
    printf("%d", idade);
    fclose(arq);
    return 0;
   //fseek();
