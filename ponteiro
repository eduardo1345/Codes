#include <stdio.h>

int main(void) {
    typedef struct {
        int dia, mes, ano;
    } data;
    printf("sizeof (data) =%d\n", sizeof(data));

    return 0;
}

/*
 * int main (void){
 * int i=1234;
 * printf("i = &d\n",i);
 * printf("&i =%ld\n",(long int) &i);
 * printf("&i=%p\n",(void *) &i);
 *
 * }
 *
 */
